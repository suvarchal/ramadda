sudo: true
language: java
services: docker
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
      - openjdk-7-jdk

script:
 - ant 
 - ls dist/
 - docker image list
 - docker run -it unidata/ramadda-docker:latest 
